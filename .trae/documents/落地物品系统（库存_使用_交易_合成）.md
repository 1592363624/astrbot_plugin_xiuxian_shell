## 目标

实现可配置、可后台编辑、强解耦的物品系统最小闭环：库存管理、物品使用（作用效果）、商店交易与基础合成；与事件系统无缝集成，统一走 `adjust_value` 数值接口。

> **《万宝楼・易宝须知》**

**天南第一楼，宝换有缘人。**

此乃天机阁所立之 \*\*【万宝楼】\*\*，为天下修士提供一处公平、便捷的以物易物之地。无论你是想寻觅炼丹之材，还是出手多余法宝，皆可在此觅得机缘。

为保交易顺畅，请诸位道友详阅以下法诀：

***

#### **一、如何上架珍宝？ (. 上架)**

若你有宝物欲与他人交换，可将其上架至万宝楼。

**【法诀】** : . 上架 <你的物品名>*<数量> 换 < 所需物品 1>*<总数 1> < 所需物品 2>\*< 总数 2> …

**【天机详解】** :

* **物品与数量** : <你的物品名>\*< 数量 > 部分，用于指定你要出售的物品和总数量。若只售一件，可省略 \*1。
* **设定总价** : 换 字之后，是你希望换回的所有物品及其**总数量** 。万宝楼会自动为你计算并挂出**单价** 或判断为**捆绑出售** 。
* **捆绑机制** : 若你设定的总价无法被上架数量整除（例如 凝血散 *3 换 妖丹* 1），此挂单将自动变为 “捆绑出售”，买家必须一次性全部购买。

**【示例】** :

1. **按件出售** : . 上架 凝血散 *100 换 灵石* 500

* 效果：上架 100 个凝血散，每件售价 5 灵石。买家可购买任意数量。

1. **捆绑出售** : . 上架 增元丹 *3 换 一阶妖丹* 1 灵石 \* 50

* 效果：上架 3 个增元丹，必须 3 个一起打包购买，总价为 1 个一阶妖丹和 50 灵石。

1. **出售单件** : . 上架 玄铁剑 换 灵石 \* 50

* 效果：上架 1 把玄铁剑，售价 50 灵石。

**注意** ：物品上架后，将从你的储物袋中移出，由万宝楼代为保管。

***

#### **二、如何淘宝寻珍？ (. 万宝楼)**

万宝楼内宝光流转，琳琅满目。你可以随意闲逛，或精准寻宝。

**【法诀】** :

* . 万宝楼：查看第一页的商品。
* . 万宝楼 <页数>: 翻到指定页数。
* . 万宝楼 搜索 <物品名>: 查找所有包含该名称的商品。
* . 万宝楼 筛选 <类型>: 筛选特定类型的商品。可用类型：**丹药、法宝、材料、图纸、种子** 。

**【天机详解】** :

* 万宝楼会清晰地展示每件商品的**挂单 ID** 、**名称数量** 、**单价 / 总价** ，以及是否有 \*\*(捆绑)\*\* 标记。

***

#### **三、如何购入宝物？ (. 购买)**

看到心仪之物，只需记下其**挂单 ID** ，便可出手购买。

**【法诀】** : . 购买 <挂单 ID>\*< 数量 >

**【天机详解】** :

* **购买部分** : 若想购买挂单中的部分物品，请务必带上 *<数量>。例如。购买 123*20。
* **购买全部** : 若想买下整个挂单，可直接使用。购买 ，或明确写出全部数量。
* **捆绑商品** : 对于标记为 (捆绑) 的商品，你只能购买全部，不可指定数量。
* **自动结算** : 你无需心算，天机阁法阵会自动根据单价和购买数量，从你的储物袋中扣除所需物品。

***

#### **四、如何打理自己的货摊？ (. 我的货摊 /. 下架)**

**【法诀】** :

* . 我的货摊：查看你当前所有正在出售的物品及其挂单 ID。
* . 下架 <挂单 ID>: 将指定 ID 的商品从万宝楼收回至你的储物袋。

## 数据结构与迁移

1. 复用现有表：`items`、`user_inventory`
2. 扩展字段（保持向后兼容）：

   * `items.properties` 规范键：`{durability_max, rarity_score, equip_slot, quality_tier}`

   * `items.effects` 存储“使用效果”数组（事件效果 DSL）；已支持

   * 需要新增表：

     * `shops(id, name, description, items_json, pricing_rule_json)`

     * `recipes(id, name, inputs_json, outputs_json, cost_json)`

   * 数据库迁移：在 `core/database/init.py` 添加建表语句与默认示例（如【固元丹】、【凝神丹】、新手商店、基础炼制配方）

## 运行态 API（core/api/items.py）

1. `get_item(item_id|name)`：查询物品元数据
2. `get_user_inventory(user_id, page, page_size)`：分页库存
3. `use_item(user_id, item_id, count=1)`：扣减并执行 `items.effects`（通过事件引擎或直接效果执行），输出修真语境文案
4. `equip_item(user_id, item_id)` / `unequip_item(user_id, item_id)`：占位（后续装备系统用）
5. `buy_item(user_id, shop_id, item_id, count)` / `sell_item(...)`：按 `shops.pricing_rule_json` 扣加灵石与物品
6. `craft(user_id, recipe_id)`：检查材料与消耗，产出物品
7. 以上所有增减统一调用 `adjust_value(user_id, target, delta, **kwargs)`，严禁重复 SQL 逻辑

## 事件节点（后端）

* 效果：

  * `use_item`（根据传入 `item_id` 调用 API 并返回效果汇总文本）

  * `buy_item` / `sell_item` / `craft`（分别调用 API）

* 条件：

  * `inventory_space`（预留）；`has_item_count`（已有）

* 触发器：复用 `command` 与 `speech_trigger`

* 前后端统一：`GenericApiEffect` 可直接调用 `core.api.items` 的函数，表单自动生成（已实现系统 API元数据）

## 后台管理 API

* 新增：`admin/api/shops.py`、`admin/api/recipes.py`

  * CRUD + JSON 字段编辑（含导入/导出）

* 强化 `admin/api/items.py`

  * 增加校验：`effects` 与 `properties` 的键名提示模板

## 前端界面

* 物品管理页增强：

  * 增加“使用效果”可视化编辑（效果数组表格）

  * 增加属性键提示与词条选择

* 新增“商店管理”页：

  * 编辑可售物品与定价公式（固定价/折扣/按稀有度）

* 新增“配方管理”页：

  * 配置输入材料、耗费（灵石/道具/时间）、输出物品

* 新增“玩家库存查看”页（只读）：

  * 支持按用户ID检索库存，便于运营排查

## 命令注册（主入口仅注册）

* `/背包`：查看自身库存（分页）

* `/使用 <物品名> [数量]`：触发 `use_item`

* `/购买 <物品名> [数量]`：默认新手商店，触发 `buy_item`

* `/出售 <物品名> [数量]`：触发 `sell_item`

* 说明：仅在 `main.py` 注册命令，业务委托 `PluginService`→`core.api.items`

## 文案与规则

* 所有提示语使用修真语境，关键信息加【】强调：如“你使用了【固元丹】（+15%稳固）”

* 物品效果聚合：支持平/百分比叠加；扣减与失败提示一致

* 栈上限：写入 `items.max_stack`，日志提示但不强制拒绝（可配置策略）

## 验证方案

1. 后台创建两枚丹药：`固元丹(+15%稳固)`、`凝神丹(+8%稳固)`；在库存发放后，命令 `/使用 固元丹` 验证效果输出与数值变更
2. 新手商店：配置【固元丹】售价，命令 `/购买 固元丹 2` 验证灵石扣减与库存增加
3. 配方：`凝神丹` 用【清灵草】等材料炼制；命令 `/炼制 凝神丹` 验证材料扣减与产出
4. 前端：在“物品管理/商店管理/配方管理/系统参数”页完成 CRUD 并观察事件效果执行

## 注意事项

* 不改动已有功能的行为；所有新能力以模块/API/事件节点形式注入

* 统一走 `adjust_value`，避免重复更新逻辑

* 数据库 WAL 与外键 ON 保持不变；新增索引按需添加

* 代码中文注释、PEP8、无未定义引用、Python 3.10 兼容

请确认后我将按此计划开始落地实现（优先：API与事件效果→后台API→前端页面→命令注册→示例数据与验证）。
