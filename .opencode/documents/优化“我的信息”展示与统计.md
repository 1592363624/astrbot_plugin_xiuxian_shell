## 问题分析

* 当前“我的信息”仅展示少数字段，缺少等级、HP/MP、当前地图等核心数据。

* “背包种类数”来源于视图`v_user_full_info.item_count`（去重的物品种类），术语不清晰，易误解为总数量。

* 同一功能存在重复实现：`PlayerHandlers.profile_info`与`BasicHandlers.handle_my_info`构造的文本一致但路径不同，建议统一由玩家领域处理器负责。

## 改动范围

* 保持`main.py`仅做命令注册，不引入业务逻辑。

* 优化`core/handlers/player_handlers.py::profile_info`的展示内容和结构。

* 新增背包统计接口：在`core/api/inventory.py`添加一个只读统计方法，返回`distinct_items`与`total_items`，避免修改数据库架构。

* 不改变`gateway`路由，仅改进返回的文本链内容。

## 展示字段（默认）

* 道号、境界与稳固值

* 等级、修为、灵石

* 气血HP、灵力MP

* 灵根名称与修为加成倍率

* 所在地图

* 储物袋物品总数量（新增）

* 进行中的任务数量

<br />

## 文案与语境

* 保持“凡人修仙传”语境，突出重点信息使用【】。

* 将“背包种类数”统一为“储物袋物品种类”，并补充“储物袋物品总数”。

##

## 验证方法

* 运行后，在群或私聊发送`我的信息`。

* 注意输出的格式,换行布局好看点

<br />

## 代码风格与规范

* 所有import置于文件顶部，遵循PEP8。

* 方法、字段、返回值均添加中文注释；避免语法警告与未定义引用。

* 不使用`requests`，仅使用已有异步栈；维持SQLite/WAL模式与External Comments SQL。

请确认以上方案，确认后我将按此实施并提交具体代码修改（含新增统计接口与展示文案优化）。
